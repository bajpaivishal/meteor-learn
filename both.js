/**
 * Created by ttnd on 24/10/16.
 */

console.log("LOADING........ in Both, Meteor.isClient = ",Meteor.isClient)
console.log("LOADING........ in Both, Meteor.isServer",Meteor.isServer)


if(Meteor.isClient){

    /// Will execute only in client

}


if(Meteor.isServer){

    /// Will execute only in Server

}
